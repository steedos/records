import { Mongo } from 'meteor/mongo'

# if (Meteor.isServer) 
# 	Instances.allow 
# 		insert: (userId, event) ->
# 			return true

# 		update: (userId, event) ->
# 			return true

# 		remove: (userId, event) ->
# 			return true