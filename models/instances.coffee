# import { Mongo } from 'meteor/mongo'
# @Instances = new Mongo.Collection('instances')

# if (Meteor.isServer) 
# 	Instances.allow 
# 		insert: (userId, event) ->
# 			return true

# 		update: (userId, event) ->
# 			return true

# 		remove: (userId, event) ->
# 			return true